# Workstream 08 M1: known module-boundary leaks as of 2026-07-30.
#
# Format: one leak per line, exactly as printed by
# scripts/check_module_boundaries.sh: "<module>:<Class> (defined in <owner>, ...)".
# This baseline is append-never in the shrinking direction: entries may be
# removed as modules are cleaned up (M2-M4), but a new leak must never be
# added silently — CI fails until either the leak is fixed (preferred) or
# this file is edited in the same commit that introduces it, so review sees
# the regression.
ATProtoMediaCore:XrpcDispatcher (defined in ATProtoXRPC, not a declared PUBLIC dependency of ATProtoMediaCore)
ATProtoMediaCore:XrpcErrorHelper (defined in ATProtoXRPC, not a declared PUBLIC dependency of ATProtoMediaCore)
ATProtoMediaCore:XrpcRoutePackServiceBag (defined in ATProtoXRPC, not a declared PUBLIC dependency of ATProtoMediaCore)
