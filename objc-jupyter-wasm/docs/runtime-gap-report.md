# Runtime gap report (superseded)

This historical hand-maintained report is superseded by the test-generated
[capability matrix](capability-matrix.md). Its former partial, broken, and missing classifications
were not kept synchronized with the runnable probes, notebooks, or compatibility corpus.

Use `bash objc-jupyter-wasm/scripts/run-capability-baseline.sh` to rebuild the kernel and regenerate
the current result table. The matrix distinguishes the validated interpreter and browser smoke
slices from the deferred compiled Objective-C execution plane.
