name: Garazyk
options:
  bundleIdPrefix: com.atproto
  deploymentTarget:
    macOS: "14.0"
  objectVersion: 56

settings:
  base:
    CLANG_ENABLE_OBJC_ARC: YES
    SDKROOT: macosx
    CLANG_C_LANGUAGE_STANDARD: c11
    CLANG_WARN_BLOCK_CAPTURE_AUTORELEASING: YES
    CLANG_WARN_DOCUMENTATION_COMMENTS: YES

targets:
  # Main CLI Tool - wrapper around CMake build
  kaszlak:
    type: tool
    platform: macOS
    sources: []
    settings:
      base:
        PRODUCT_NAME: kaszlak
        INFOPLIST_FILE: Garazyk/Resources/Info.plist
        MACOSX_DEPLOYMENT_TARGET: "14.0"
        EXECUTABLE_PATH: "$(PROJECT_DIR)/build/bin/kaszlak"
    prebuildScripts:
        - name: "Build with CMake"
          basedOnDependencyAnalysis: false
          script: |
            #!/bin/bash
            set -e
            mkdir -p "${PROJECT_DIR}/build"
            cd "${PROJECT_DIR}/build"
            XCODE_CLANG="$(xcrun -find clang)"
            XCODE_CLANGXX="$(xcrun -find clang++)"
            # CMake does not allow changing compilers in an existing build directory.
            # If this build dir was configured via ~/.swiftly/bin/cc, reset it.
            if [ -f CMakeCache.txt ] && grep -q "/\\.swiftly/bin/cc" CMakeCache.txt; then
              rm -f CMakeCache.txt
              rm -rf CMakeFiles
            fi
            # Configure and build
            cmake .. \
              -DCMAKE_BUILD_TYPE=${CONFIGURATION} \
              -DCMAKE_OSX_DEPLOYMENT_TARGET=14.0 \
              -DCMAKE_C_COMPILER="${XCODE_CLANG}" \
              -DCMAKE_CXX_COMPILER="${XCODE_CLANGXX}" \
              -DCMAKE_OBJC_COMPILER="${XCODE_CLANG}" \
              -DCMAKE_OBJCXX_COMPILER="${XCODE_CLANGXX}" \
              -DBUILD_SECP256K1=ON \
              -DBUILD_TESTS=OFF \
              -DBUILD_FUZZERS=OFF
            cmake --build . --target kaszlak --parallel $(sysctl -n hw.ncpu)

  # Standalone PLC Server
  campagnola:
    type: tool
    platform: macOS
    sources: []
    settings:
      base:
        PRODUCT_NAME: campagnola
        INFOPLIST_FILE: Garazyk/Resources/Info.plist
        MACOSX_DEPLOYMENT_TARGET: "14.0"
        EXECUTABLE_PATH: "$(PROJECT_DIR)/build/bin/campagnola"
    prebuildScripts:
        - name: "Build PLC with CMake"
          basedOnDependencyAnalysis: false
          script: |
            #!/bin/bash
            set -e
            mkdir -p "${PROJECT_DIR}/build"
            cd "${PROJECT_DIR}/build"
            XCODE_CLANG="$(xcrun -find clang)"
            XCODE_CLANGXX="$(xcrun -find clang++)"
            if [ -f CMakeCache.txt ] && grep -q "/\\.swiftly/bin/cc" CMakeCache.txt; then
              rm -f CMakeCache.txt
              rm -rf CMakeFiles
            fi
            # Configure and build
            cmake .. \
              -DCMAKE_BUILD_TYPE=${CONFIGURATION} \
              -DCMAKE_OSX_DEPLOYMENT_TARGET=14.0 \
              -DCMAKE_C_COMPILER="${XCODE_CLANG}" \
              -DCMAKE_CXX_COMPILER="${XCODE_CLANGXX}" \
              -DCMAKE_OBJC_COMPILER="${XCODE_CLANG}" \
              -DCMAKE_OBJCXX_COMPILER="${XCODE_CLANGXX}" \
              -DBUILD_SECP256K1=ON \
              -DBUILD_TESTS=OFF \
              -DBUILD_FUZZERS=OFF
            cmake --build . --target campagnola --parallel $(sysctl -n hw.ncpu)

  # Standalone Relay Server
  zuk:
    type: tool
    platform: macOS
    sources: []
    settings:
      base:
        PRODUCT_NAME: zuk
        INFOPLIST_FILE: Garazyk/Resources/Info.plist
        MACOSX_DEPLOYMENT_TARGET: "14.0"
        EXECUTABLE_PATH: "$(PROJECT_DIR)/build/bin/zuk"
    prebuildScripts:
        - name: "Build Zuk with CMake"
          basedOnDependencyAnalysis: false
          script: |
            #!/bin/bash
            set -e
            mkdir -p "${PROJECT_DIR}/build"
            cd "${PROJECT_DIR}/build"
            XCODE_CLANG="$(xcrun -find clang)"
            XCODE_CLANGXX="$(xcrun -find clang++)"
            if [ -f CMakeCache.txt ] && grep -q "/\\.swiftly/bin/cc" CMakeCache.txt; then
              rm -f CMakeCache.txt
              rm -rf CMakeFiles
            fi
            # Configure and build
            cmake .. \
              -DCMAKE_BUILD_TYPE=${CONFIGURATION} \
              -DCMAKE_OSX_DEPLOYMENT_TARGET=14.0 \
              -DCMAKE_C_COMPILER="${XCODE_CLANG}" \
              -DCMAKE_CXX_COMPILER="${XCODE_CLANGXX}" \
              -DCMAKE_OBJC_COMPILER="${XCODE_CLANG}" \
              -DCMAKE_OBJCXX_COMPILER="${XCODE_CLANGXX}" \
              -DBUILD_SECP256K1=ON \
              -DBUILD_TESTS=OFF \
              -DBUILD_FUZZERS=OFF
            cmake --build . --target zuk --parallel $(sysctl -n hw.ncpu)

  # Dedicated Admin UI Service
  garazyk-ui:
    type: tool
    platform: macOS
    sources: []
    settings:
      base:
        PRODUCT_NAME: garazyk-ui
        INFOPLIST_FILE: Garazyk/Resources/Info.plist
        MACOSX_DEPLOYMENT_TARGET: "14.0"
        EXECUTABLE_PATH: "$(PROJECT_DIR)/build/bin/garazyk-ui"
    prebuildScripts:
        - name: "Build garazyk-ui with CMake"
          basedOnDependencyAnalysis: false
          script: |
            #!/bin/bash
            set -e
            mkdir -p "${PROJECT_DIR}/build"
            cd "${PROJECT_DIR}/build"
            XCODE_CLANG="$(xcrun -find clang)"
            XCODE_CLANGXX="$(xcrun -find clang++)"
            if [ -f CMakeCache.txt ] && grep -q "/\\.swiftly/bin/cc" CMakeCache.txt; then
              rm -f CMakeCache.txt
              rm -rf CMakeFiles
            fi
            # Configure and build
            cmake .. \
              -DCMAKE_BUILD_TYPE=${CONFIGURATION} \
              -DCMAKE_OSX_DEPLOYMENT_TARGET=14.0 \
              -DCMAKE_C_COMPILER="${XCODE_CLANG}" \
              -DCMAKE_CXX_COMPILER="${XCODE_CLANGXX}" \
              -DCMAKE_OBJC_COMPILER="${XCODE_CLANG}" \
              -DCMAKE_OBJCXX_COMPILER="${XCODE_CLANGXX}" \
              -DBUILD_SECP256K1=ON \
              -DBUILD_TESTS=OFF \
              -DBUILD_FUZZERS=OFF
            cmake --build . --target garazyk-ui --parallel $(sysctl -n hw.ncpu)

  # Dedicated Chat Service
  syrena-chat:
    type: tool
    platform: macOS
    sources: []
    settings:
      base:
        PRODUCT_NAME: syrena-chat
        INFOPLIST_FILE: Garazyk/Resources/Info.plist
        MACOSX_DEPLOYMENT_TARGET: "14.0"
        EXECUTABLE_PATH: "$(PROJECT_DIR)/build/bin/syrena-chat"
    prebuildScripts:
        - name: "Build syrena-chat with CMake"
          basedOnDependencyAnalysis: false
          script: |
            #!/bin/bash
            set -e
            mkdir -p "${PROJECT_DIR}/build"
            cd "${PROJECT_DIR}/build"
            XCODE_CLANG="$(xcrun -find clang)"
            XCODE_CLANGXX="$(xcrun -find clang++)"
            if [ -f CMakeCache.txt ] && grep -q "/\\.swiftly/bin/cc" CMakeCache.txt; then
              rm -f CMakeCache.txt
              rm -rf CMakeFiles
            fi
            # Configure and build
            cmake .. \
              -DCMAKE_BUILD_TYPE=${CONFIGURATION} \
              -DCMAKE_OSX_DEPLOYMENT_TARGET=14.0 \
              -DCMAKE_C_COMPILER="${XCODE_CLANG}" \
              -DCMAKE_CXX_COMPILER="${XCODE_CLANGXX}" \
              -DCMAKE_OBJC_COMPILER="${XCODE_CLANG}" \
              -DCMAKE_OBJCXX_COMPILER="${XCODE_CLANGXX}" \
              -DBUILD_SECP256K1=ON \
              -DBUILD_TESTS=OFF \
              -DBUILD_FUZZERS=OFF
            cmake --build . --target syrena-chat --parallel $(sysctl -n hw.ncpu)

  # Test Runner - wrapper around CMake build
  AllTests:
    type: tool
    platform: macOS
    sources: []
    settings:
      base:
        PRODUCT_NAME: AllTests
        INFOPLIST_FILE: Garazyk/Resources/Info.plist
        MACOSX_DEPLOYMENT_TARGET: "14.0"
        EXECUTABLE_PATH: "$(PROJECT_DIR)/build/tests/AllTests"
    prebuildScripts:
        - name: "Build Tests with CMake"
          basedOnDependencyAnalysis: false
          script: |
            #!/bin/bash
            set -e
            mkdir -p "${PROJECT_DIR}/build"
            cd "${PROJECT_DIR}/build"
            XCODE_CLANG="$(xcrun -find clang)"
            XCODE_CLANGXX="$(xcrun -find clang++)"
            if [ -f CMakeCache.txt ] && grep -q "/\\.swiftly/bin/cc" CMakeCache.txt; then
              rm -f CMakeCache.txt
              rm -rf CMakeFiles
            fi
            # Configure and build tests
            cmake .. \
              -DCMAKE_BUILD_TYPE=${CONFIGURATION} \
              -DCMAKE_OSX_DEPLOYMENT_TARGET=14.0 \
              -DCMAKE_C_COMPILER="${XCODE_CLANG}" \
              -DCMAKE_CXX_COMPILER="${XCODE_CLANGXX}" \
              -DCMAKE_OBJC_COMPILER="${XCODE_CLANG}" \
              -DCMAKE_OBJCXX_COMPILER="${XCODE_CLANGXX}" \
              -DBUILD_SECP256K1=ON \
              -DBUILD_TESTS=ON \
              -DBUILD_FUZZERS=OFF
            cmake --build . --target AllTests --parallel $(sysctl -n hw.ncpu)

  # Fuzzers - aggregate target to build all fuzzers
  Fuzzers:
    type: tool
    platform: macOS
    sources: []
    settings:
      base:
        PRODUCT_NAME: Fuzzers
    prebuildScripts:
        - name: "Build Fuzzers with CMake"
          basedOnDependencyAnalysis: false
          script: |
            #!/bin/bash
            set -e
            mkdir -p "${PROJECT_DIR}/build"
            cd "${PROJECT_DIR}/build"
            XCODE_CLANG="$(xcrun -find clang)"
            XCODE_CLANGXX="$(xcrun -find clang++)"
            if [ -f CMakeCache.txt ] && grep -q "/\\.swiftly/bin/cc" CMakeCache.txt; then
              rm -f CMakeCache.txt
              rm -rf CMakeFiles
            fi
            # Configure and build fuzzers
            cmake .. \
              -DCMAKE_BUILD_TYPE=${CONFIGURATION} \
              -DCMAKE_OSX_DEPLOYMENT_TARGET=14.0 \
              -DCMAKE_C_COMPILER="${XCODE_CLANG}" \
              -DCMAKE_CXX_COMPILER="${XCODE_CLANGXX}" \
              -DCMAKE_OBJC_COMPILER="${XCODE_CLANG}" \
              -DCMAKE_OBJCXX_COMPILER="${XCODE_CLANGXX}" \
              -DBUILD_SECP256K1=ON \
              -DBUILD_TESTS=OFF \
              -DBUILD_FUZZERS=ON
            cmake --build . --parallel $(sysctl -n hw.ncpu) --target fuzz_xrpc fuzz_cbor fuzz_http fuzz_jwt fuzz_jwt_struct fuzz_http_struct fuzz_dpop fuzz_mime fuzz_sqlite fuzz_lexicon fuzz_mst
