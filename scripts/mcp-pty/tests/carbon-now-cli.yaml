# Auto-generated scenario for carbon-now-cli (ink/image-gen)
# Framework confidence via semantic detection will be verified
# UI Patterns: form-input, preview, select
name: carbon-now-cli image gen
description: "Launch carbon-now-cli, verify semantic detection, exercise image-gen patterns, then quit"
command: /opt/homebrew/bin/carbon-now-cli
cols: 80
rows: 24
settleMs: 2000
steps:
  - type: wait
    timeoutMs: 2000
    maxMs: 6000
    label: Wait for carbon-now-cli to animate
  - type: observe
    maxMs: 500
    label: Take semantic snapshot
  - type: assert_semantic
    target: app
    expected: carbon-now-cli
    maxMs: 500
    label: Verify app is carbon-now-cli
  - type: quit
    maxMs: 2000
    label: Quit carbon-now-cli
  - type: wait
    timeoutMs: 500
    maxMs: 1500
    label: Wait for exit
