# Auto-generated scenario for ncdirect (notcurses/renderer)
# Framework confidence via semantic detection will be verified
# UI Patterns: sixels, multimedia, sprites
name: ncdirect renderer
description: "Launch ncdirect, verify semantic detection, exercise renderer patterns, then quit"
command: /opt/homebrew/bin/ncdirect
cols: 80
rows: 24
settleMs: 2000
steps:
  - type: wait
    timeoutMs: 2000
    maxMs: 6000
    label: Wait for ncdirect to animate
  - type: observe
    maxMs: 500
    label: Take semantic snapshot
  - type: assert_semantic
    target: app
    expected: ncdirect
    maxMs: 500
    label: Verify app is ncdirect
  - type: quit
    maxMs: 2000
    label: Quit ncdirect
  - type: wait
    timeoutMs: 500
    maxMs: 1500
    label: Wait for exit
