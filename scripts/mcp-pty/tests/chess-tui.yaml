# Auto-generated scenario for chess-tui (ratatui/game)
# Framework confidence via semantic detection will be verified
# UI Patterns: board, entities, game-loop, move-list, timer
name: chess-tui game
description: "Launch chess-tui, verify semantic detection, exercise game patterns, then quit"
command: /opt/homebrew/bin/chess-tui
cols: 80
rows: 24
settleMs: 2000
steps:
  - type: wait
    timeoutMs: 2000
    label: Wait for chess-tui to render
  - type: observe
    label: Take semantic snapshot
  - type: assert_semantic
    target: app
    expected: chess-tui
    label: Verify app is chess-tui
  - type: press_key
    value: j
    label: Move down
  - type: wait
    timeoutMs: 200
    label: Brief pause
  - type: assert_cursor_moved
    label: Verify cursor moved
  - type: press_key
    value: k
    label: Move up
  - type: wait
    timeoutMs: 200
    label: Brief pause
  - type: press_key
    value: l
    label: Move right
  - type: wait
    timeoutMs: 200
    label: Brief pause
  - type: press_key
    value: h
    label: Move left
  - type: wait
    timeoutMs: 200
    label: Brief pause
  - type: quit
    label: Quit chess-tui
  - type: wait
    timeoutMs: 1000
    label: Wait for exit
