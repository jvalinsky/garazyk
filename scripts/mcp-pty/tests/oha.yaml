# Auto-generated scenario for oha (ratatui/network-tool)
# Framework confidence via semantic detection will be verified
# UI Patterns: chart, gauge, real-time-data, progress-bar
name: oha network tool
description: "Launch oha, verify semantic detection, exercise network-tool patterns, then quit"
command: /opt/homebrew/bin/oha
cols: 80
rows: 24
settleMs: 2000
steps:
  - type: wait
    timeoutMs: 3000
    maxMs: 9000
    label: Wait for oha to render
  - type: observe
    maxMs: 500
    label: Take semantic snapshot
  - type: assert_semantic
    target: app
    expected: oha
    maxMs: 500
    label: Verify app is oha
  - type: press_key
    value: j
    maxMs: 500
    label: Navigate down
  - type: wait
    timeoutMs: 200
    maxMs: 1500
    label: Brief pause
  - type: assert_cursor_moved
    maxMs: 500
    label: Verify cursor moved
  - type: press_key
    value: k
    maxMs: 500
    label: Navigate up
  - type: wait
    timeoutMs: 200
    maxMs: 1500
    label: Brief pause
  - type: quit
    maxMs: 2000
    label: Quit oha
  - type: wait
    timeoutMs: 1000
    maxMs: 3000
    label: Wait for exit
