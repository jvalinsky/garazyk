# Auto-generated scenario for lazygit (bubbletea/git-client)
# Framework confidence via semantic detection will be verified
# UI Patterns: split-pane, list-selection, tab-navigation, text-input, popup-modal, filter-search, confirm-dialog
name: lazygit git client
description: "Launch lazygit, verify semantic detection, exercise git-client patterns, then quit"
command: /opt/homebrew/bin/lazygit
cols: 80
rows: 24
settleMs: 2000
steps:
  - type: wait
    timeoutMs: 1500
    maxMs: 4500
    label: Wait for lazygit to render
  - type: observe
    maxMs: 500
    label: Take semantic snapshot
  - type: assert_semantic
    target: app
    expected: lazygit
    maxMs: 500
    label: Verify app is lazygit
  - type: assert_semantic
    target: framework
    expected: bubbletea
    maxMs: 500
    label: Verify framework is bubbletea
  - type: press_key
    value: "1"
    maxMs: 500
    label: Switch to first panel
  - type: wait
    timeoutMs: 300
    maxMs: 1500
    label: Brief pause
  - type: press_key
    value: "2"
    maxMs: 500
    label: Switch to second panel
  - type: wait
    timeoutMs: 300
    maxMs: 1500
    label: Brief pause
  - type: press_key
    value: j
    maxMs: 500
    label: Navigate down in list
  - type: wait
    timeoutMs: 200
    maxMs: 1500
    label: Brief pause
  - type: press_key
    value: k
    maxMs: 500
    label: Navigate up in list
  - type: wait
    timeoutMs: 200
    maxMs: 1500
    label: Brief pause
  - type: assert_cursor_moved
    maxMs: 500
    label: Verify cursor moved (at least 1 of 2 directions)
  - type: quit
    maxMs: 2000
    label: Quit lazygit
  - type: wait
    timeoutMs: 1000
    maxMs: 3000
    label: Wait for exit
