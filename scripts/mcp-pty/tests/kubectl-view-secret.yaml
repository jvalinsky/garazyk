# Auto-generated scenario for kubectl-view-secret (ratatui/kubernetes)
# Framework confidence via semantic detection will be verified
# UI Patterns: table, form-input, masked-view, tree-view
name: kubectl-view-secret kubernetes
description: "Launch kubectl-view-secret, verify semantic detection, exercise kubernetes patterns, then quit"
command: /opt/homebrew/bin/kubectl-view-secret
cols: 80
rows: 24
settleMs: 2000
steps:
  - type: wait
    timeoutMs: 1000
    label: Wait for kubectl-view-secret to render
  - type: observe
    label: Take semantic snapshot
  - type: assert_semantic
    target: app
    expected: kubectl-view-secret
    label: Verify app is kubectl-view-secret
  - type: assert_semantic
    target: framework
    expected: ratatui
    label: Verify framework is ratatui
  - type: quit
    label: Quit kubectl-view-secret
  - type: wait
    timeoutMs: 1000
    label: Wait for exit
