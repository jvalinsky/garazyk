# Auto-generated scenario for textual-markdown (textual/markdown)
# Framework confidence via semantic detection will be verified
# UI Patterns: render, scroll, link
name: textual-markdown markdown
description: "Launch textual-markdown, verify semantic detection, exercise markdown patterns, then quit"
command: textual-markdown
cols: 80
rows: 24
settleMs: 1000
steps:
  - type: wait
    timeoutMs: 1000
    label: Wait for textual-markdown to render
  - type: observe
    label: Take semantic snapshot
  - type: assert_semantic
    target: app
    expected: textual-markdown
    label: Verify app is textual-markdown
  - type: press_key
    value: j
    label: Scroll down
  - type: wait
    timeoutMs: 200
    label: Brief pause
  - type: press_key
    value: k
    label: Scroll up
  - type: wait
    timeoutMs: 200
    label: Brief pause
  - type: quit
    label: Quit textual-markdown
  - type: wait
    timeoutMs: 500
    label: Wait for exit
