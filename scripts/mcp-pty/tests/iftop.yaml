# Auto-generated scenario for iftop (ncurses/network-tool)
# Framework confidence via semantic detection will be verified
# UI Patterns: chart, list-selection, real-time-data, sort
name: iftop network tool
description: "Launch iftop, verify semantic detection, exercise network-tool patterns, then quit"
command: /opt/homebrew/sbin/iftop
cols: 80
rows: 24
settleMs: 2000
steps:
  - type: wait
    timeoutMs: 3000
    maxMs: 9000
    label: Wait for iftop to render
  - type: observe
    maxMs: 500
    label: Take semantic snapshot
  - type: assert_semantic
    target: app
    expected: iftop
    maxMs: 500
    label: Verify app is iftop
  - type: press_key
    value: j
    maxMs: 500
    label: Navigate down
  - type: wait
    timeoutMs: 200
    maxMs: 1500
    label: Brief pause
  - type: press_key
    value: k
    maxMs: 500
    label: Navigate up
  - type: wait
    timeoutMs: 200
    maxMs: 1500
    label: Brief pause
  - type: assert_cursor_moved
    maxMs: 500
    label: Verify cursor moved (at least 1 of 2 directions)
  - type: quit
    maxMs: 2000
    label: Quit iftop
  - type: wait
    timeoutMs: 1000
    maxMs: 3000
    label: Wait for exit
