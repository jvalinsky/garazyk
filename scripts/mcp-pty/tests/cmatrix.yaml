# Auto-generated scenario for cmatrix (ncurses/art)
# Framework confidence via semantic detection will be verified
# UI Patterns: animation, styling, scroll
name: cmatrix art
description: "Launch cmatrix, verify semantic detection, exercise art patterns, then quit"
command: /opt/homebrew/bin/cmatrix
cols: 80
rows: 24
settleMs: 2000
steps:
  - type: wait
    timeoutMs: 2000
    maxMs: 6000
    label: Wait for cmatrix to animate
  - type: observe
    maxMs: 500
    label: Take semantic snapshot
  - type: assert_semantic
    target: app
    expected: cmatrix
    maxMs: 500
    label: Verify app is cmatrix
  - type: quit
    maxMs: 2000
    label: Quit cmatrix
  - type: wait
    timeoutMs: 500
    maxMs: 1500
    label: Wait for exit
