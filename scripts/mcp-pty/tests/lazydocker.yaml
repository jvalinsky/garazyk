# Auto-generated scenario for lazydocker (bubbletea/docker)
# Framework confidence via semantic detection will be verified
# UI Patterns: tab-navigation, list-selection, log-viewer, split-pane, status-bar
name: lazydocker docker
description: "Launch lazydocker, verify semantic detection, exercise docker patterns, then quit"
command: /opt/homebrew/bin/lazydocker
cols: 80
rows: 24
settleMs: 2000
steps:
  - type: wait
    timeoutMs: 1000
    label: Wait for lazydocker to render
  - type: observe
    label: Take semantic snapshot
  - type: assert_semantic
    target: app
    expected: lazydocker
    label: Verify app is lazydocker
  - type: assert_semantic
    target: framework
    expected: bubbletea
    label: Verify framework is bubbletea
  - type: press_key
    value: tab
    label: Switch to next tab
  - type: wait
    timeoutMs: 500
    label: Brief pause
  - type: assert_content_changed
    label: Verify content changed after tab switch
  - type: press_key
    value: tab
    label: Switch to third tab
  - type: wait
    timeoutMs: 500
    label: Brief pause
  - type: press_key
    value: j
    label: Navigate down
  - type: wait
    timeoutMs: 200
    label: Brief pause
  - type: assert_cursor_moved
    label: Verify cursor moved after navigation
  - type: press_key
    value: k
    label: Navigate up
  - type: wait
    timeoutMs: 200
    label: Brief pause
  - type: quit
    label: Quit lazydocker
  - type: wait
    timeoutMs: 1000
    label: Wait for exit
