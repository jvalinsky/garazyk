# Auto-generated scenario for httpie (rich/api-client)
# Framework confidence via semantic detection will be verified
# UI Patterns: syntax-highlight, table, form-input
name: httpie api client
description: "Launch httpie, verify semantic detection, exercise api-client patterns, then quit"
command: http
cols: 80
rows: 24
settleMs: 2000
steps:
  - type: wait
    timeoutMs: 3000
    maxMs: 9000
    label: Wait for httpie to render
  - type: observe
    maxMs: 500
    label: Take semantic snapshot
  - type: assert_semantic
    target: app
    expected: httpie
    maxMs: 500
    label: Verify app is httpie
  - type: assert_semantic
    target: framework
    expected: rich
    maxMs: 500
    label: Verify framework is rich
  - type: quit
    maxMs: 2000
    label: Quit httpie
  - type: wait
    timeoutMs: 1000
    maxMs: 3000
    label: Wait for exit
