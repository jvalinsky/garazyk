# Auto-generated scenario for gum (bubbletea/form-tool)
# Framework confidence via semantic detection will be verified
# UI Patterns: form-input, picker, confirm-dialog, filter-search
name: gum form tool
description: "Launch gum, verify semantic detection, exercise form-tool patterns, then quit"
command: /opt/homebrew/bin/gum
args: choose, --help
cols: 80
rows: 24
settleMs: 2000
steps:
  - type: wait
    timeoutMs: 1500
    label: Wait for gum to render
  - type: observe
    label: Take semantic snapshot
  - type: assert_semantic
    target: app
    expected: gum-form
    label: Verify app is gum-form
  - type: assert_capability
    target: quit.keys
    contains: q, Esc
    label: Verify quit key is available
  - type: quit
    label: Quit gum
  - type: wait
    timeoutMs: 1000
    label: Wait for exit
