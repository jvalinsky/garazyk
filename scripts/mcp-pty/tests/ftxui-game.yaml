# Auto-generated scenario for ftxui-game (ftxui/game)
# Framework confidence via semantic detection will be verified
# UI Patterns: board, player, score, entities
name: ftxui-game game
description: "Launch ftxui-game, verify semantic detection, exercise game patterns, then quit"
command: /opt/homebrew/bin/ftxui-game
cols: 80
rows: 24
settleMs: 2000
steps:
  - type: wait
    timeoutMs: 2000
    label: Wait for ftxui-game to render
  - type: observe
    label: Take semantic snapshot
  - type: assert_semantic
    target: app
    expected: ftxui-game
    label: Verify app is ftxui-game
  - type: press_key
    value: j
    label: Move down
  - type: wait
    timeoutMs: 200
    label: Brief pause
  - type: assert_cursor_moved
    label: Verify cursor moved
  - type: press_key
    value: k
    label: Move up
  - type: wait
    timeoutMs: 200
    label: Brief pause
  - type: press_key
    value: l
    label: Move right
  - type: wait
    timeoutMs: 200
    label: Brief pause
  - type: press_key
    value: h
    label: Move left
  - type: wait
    timeoutMs: 200
    label: Brief pause
  - type: quit
    label: Quit ftxui-game
  - type: wait
    timeoutMs: 1000
    label: Wait for exit
