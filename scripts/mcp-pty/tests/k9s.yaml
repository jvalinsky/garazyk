# Auto-generated scenario for k9s (bubbletea/kubernetes)
# Framework confidence via semantic detection will be verified
# UI Patterns: table, tab-navigation, log-viewer, search, confirm-dialog
name: k9s kubernetes
description: "Launch k9s, verify semantic detection, exercise kubernetes patterns, then quit"
command: /opt/homebrew/bin/k9s
cols: 80
rows: 24
settleMs: 2000
steps:
  - type: wait
    timeoutMs: 1000
    maxMs: 3000
    label: Wait for k9s to render
  - type: observe
    maxMs: 500
    label: Take semantic snapshot
  - type: assert_semantic
    target: app
    expected: k9s
    maxMs: 500
    label: Verify app is k9s
  - type: assert_semantic
    target: framework
    expected: bubbletea
    maxMs: 500
    label: Verify framework is bubbletea
  - type: press_key
    value: tab
    maxMs: 500
    label: Switch to next tab
  - type: wait
    timeoutMs: 500
    maxMs: 1500
    label: Brief pause
  - type: assert_content_changed
    maxMs: 500
    label: Verify content changed after tab switch
  - type: press_key
    value: tab
    maxMs: 500
    label: Switch to third tab
  - type: wait
    timeoutMs: 500
    maxMs: 1500
    label: Brief pause
  - type: quit
    maxMs: 3000
    label: Quit k9s
  - type: wait
    timeoutMs: 1000
    maxMs: 3000
    label: Wait for exit
