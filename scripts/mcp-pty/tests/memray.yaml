# Auto-generated scenario for memray (textual/profiler)
# Framework confidence via semantic detection will be verified
# UI Patterns: chart, table, flame-graph, tab-navigation
name: memray profiler
description: "Launch memray, verify semantic detection, exercise profiler patterns, then quit"
command: memray
cols: 80
rows: 24
settleMs: 2000
steps:
  - type: wait
    timeoutMs: 1000
    maxMs: 3000
    label: Wait for memray to render
  - type: observe
    maxMs: 500
    label: Take semantic snapshot
  - type: assert_semantic
    target: app
    expected: memray
    maxMs: 500
    label: Verify app is memray
  - type: assert_semantic
    target: framework
    expected: textual
    maxMs: 500
    label: Verify framework is textual
  - type: press_key
    value: tab
    maxMs: 500
    label: Switch to next tab
  - type: wait
    timeoutMs: 500
    maxMs: 1500
    label: Brief pause
  - type: assert_content_changed
    maxMs: 500
    label: Verify content changed after tab switch
  - type: press_key
    value: tab
    maxMs: 500
    label: Switch to third tab
  - type: wait
    timeoutMs: 500
    maxMs: 1500
    label: Brief pause
  - type: quit
    maxMs: 2000
    label: Quit memray
  - type: wait
    timeoutMs: 1000
    maxMs: 3000
    label: Wait for exit
