# Auto-generated scenario for bastet (ncurses/game)
# Framework confidence via semantic detection will be verified
# UI Patterns: board, score, entities, game-loop
name: bastet game
description: "Launch bastet, verify semantic detection, exercise game patterns, then quit"
command: /opt/homebrew/bin/bastet
cols: 80
rows: 24
settleMs: 2000
steps:
  - type: wait
    timeoutMs: 2000
    label: Wait for bastet to render
  - type: observe
    label: Take semantic snapshot
  - type: assert_semantic
    target: app
    expected: bastet
    label: Verify app is bastet
  - type: press_key
    value: j
    label: Move down
  - type: wait
    timeoutMs: 200
    label: Brief pause
  - type: assert_cursor_moved
    label: Verify cursor moved
  - type: press_key
    value: k
    label: Move up
  - type: wait
    timeoutMs: 200
    label: Brief pause
  - type: press_key
    value: l
    label: Move right
  - type: wait
    timeoutMs: 200
    label: Brief pause
  - type: press_key
    value: h
    label: Move left
  - type: wait
    timeoutMs: 200
    label: Brief pause
  - type: quit
    label: Quit bastet
  - type: wait
    timeoutMs: 1000
    label: Wait for exit
