name: CI

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Setup repo
        uses: actions/checkout@v4

      - name: Setup Deno
        uses: denoland/setup-deno@v2
        with:
          deno-version: v2.2.x

      - name: Format
        run: deno fmt --check packages/ scripts/

      - name: Lint
        run: deno lint packages/ scripts/

      - name: Type Check
        run: |
          deno check packages/docker-client/mod.ts
          deno check packages/atproto-client/mod.ts
          deno check packages/atproto-topology/mod.ts
          deno check packages/scenario-runner/mod.ts
          deno check scripts/run_scenarios.ts

      - name: Run Tests
        run: deno test -A packages/

      - name: Publish Dry Run
        run: |
          for pkg in docker-client atproto-client atproto-topology scenario-runner; do
            echo "--- $pkg ---"
            cd packages/$pkg
            if [ "$pkg" = "atproto-topology" ]; then
              deno publish --dry-run --allow-slow-types
            else
              deno publish --dry-run
            fi
            cd ../..
          done
