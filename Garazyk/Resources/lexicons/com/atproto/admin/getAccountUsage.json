{
  "lexicon": 1,
  "id": "com.atproto.admin.getAccountUsage",
  "defs": {
    "main": {
      "type": "query",
      "description": "Get storage usage metrics for an account. Admin-only.",
      "parameters": {
        "type": "params",
        "required": ["did"],
        "properties": {
          "did": { "type": "string", "format": "did" }
        }
      },
      "output": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "required": ["did", "blobBytes", "blobCount", "repoBytes", "recordCount"],
          "properties": {
            "did": { "type": "string", "format": "did" },
            "blobBytes": { "type": "integer", "description": "Total blob storage bytes for this account." },
            "blobCount": { "type": "integer", "description": "Total number of blobs for this account." },
            "repoBytes": { "type": "integer", "description": "Total repo block storage bytes for this account." },
            "recordCount": { "type": "integer", "description": "Total number of records for this account." }
          }
        }
      }
    }
  }
}