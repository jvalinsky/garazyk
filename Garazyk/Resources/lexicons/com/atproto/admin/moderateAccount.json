{
  "lexicon": 1,
  "id": "com.atproto.admin.moderateAccount",
  "defs": {
    "main": {
      "type": "procedure",
      "description": "Legacy compatibility endpoint for account moderation decisions.",
      "input": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "required": ["did", "action"],
          "properties": {
            "did": { "type": "string", "format": "did" },
            "action": { "type": "string" },
            "reason": { "type": "string" },
            "note": { "type": "string" }
          }
        }
      },
      "output": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "required": ["status", "did", "action", "timestamp"],
          "properties": {
            "status": { "type": "string" },
            "did": { "type": "string", "format": "did" },
            "action": { "type": "string" },
            "timestamp": { "type": "string", "format": "datetime" }
          }
        }
      }
    }
  }
}
