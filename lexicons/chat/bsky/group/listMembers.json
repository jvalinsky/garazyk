{
  "lexicon": 1,
  "id": "chat.bsky.group.listMembers",
  "defs": {
    "main": {
      "type": "query",
      "description": "List group members",
      "parameters": {
        "type": "params",
        "required": ["groupUri"],
        "properties": {
          "groupUri": { "type": "string" },
          "limit": { "type": "integer" },
          "cursor": { "type": "string" }
        }
      },
      "output": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "properties": {
            "members": { "type": "array", "items": { "type": "object" } },
            "cursor": { "type": "string" }
          }
        }
      }
    }
  }
}
